---
import Layout from '../../layouts/Layout.astro';
import { tools, getActiveCategories, getCategorySlug } from '../../data/registry';

export function getStaticPaths() {
  return getActiveCategories().map((cat) => {
    const categorySlug = getCategorySlug(cat.name);
    return {
      params: { category: categorySlug },
      props: { categoryData: cat }
    };
  });
}

const { categoryData } = Astro.props;
const categoryTools = tools.filter(t => t.category === categoryData.name);
const categorySlug = getCategorySlug(categoryData.name);

const collectionSchema = {
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  "name": `${categoryData.name} Tools`,
  "description": `${categoryData.name} tools and calculators.`,
  "url": `https://studentkit.in/category/${categorySlug}`
};

const breadcrumbSchema = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    {
      "@type": "ListItem",
      "position": 1,
      "name": "Home",
      "item": "https://studentkit.in/"
    },
    {
      "@type": "ListItem",
      "position": 2,
      "name": categoryData.name,
      "item": `https://studentkit.in/category/${categorySlug}`
    }
  ]
};
---

<Layout title={categoryData.name} description={`${categoryData.name} tools and calculators for students.`}>
  <Fragment slot="head">
    <script type="application/ld+json" set:html={JSON.stringify(collectionSchema)}></script>
    <script type="application/ld+json" set:html={JSON.stringify(breadcrumbSchema)}></script>
  </Fragment>

  <!-- Breadcrumbs -->
  <nav class="flex text-xs text-slate-500 mb-3" aria-label="Breadcrumb">
    <ol class="inline-flex items-center space-x-1.5">
      <li class="inline-flex items-center">
        <a href="/" class="inline-flex items-center hover:text-blue-600 transition-colors">
          Home
        </a>
      </li>
      <li aria-current="page" class="flex items-center">
        <svg class="w-3.5 h-3.5 text-slate-400 mx-1" fill="currentColor" viewBox="0 0 20 20">
          <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"></path>
        </svg>
        <span class="text-slate-800 font-medium">{categoryData.name}</span>
      </li>
    </ol>
  </nav>

  <!-- Category Header (Minimal) -->
  <div class="mb-4 text-left">
    <h1 class="text-2xl sm:text-3xl font-extrabold text-slate-900 tracking-tight">{categoryData.name}</h1>
  </div>

  <!-- Single Consolidated Tools Collection (Compact) -->
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-2.5 sm:gap-3">
    {categoryTools.map(tool => (
      <a
        href={`/tools/${tool.slug}`}
        class="group flex items-center gap-3 p-3 bg-white rounded-xl border border-slate-200/80 hover:border-blue-500 hover:shadow-xs transition-all min-h-[44px]"
      >
        <div class="flex items-center justify-center w-6 h-6 rounded-md bg-blue-50/80 text-blue-600 shrink-0 group-hover:bg-blue-600 group-hover:text-white transition-colors [&>svg]:w-3.5 [&>svg]:h-3.5" set:html={tool.icon} />
        <span class="text-[13px] sm:text-sm font-semibold text-slate-900 group-hover:text-blue-600 transition-colors leading-snug">
          {tool.title}
        </span>
      </a>
    ))}
  </div>
</Layout>
