---
export interface Props {
  tool?: any;
}
---

<div class="space-y-3 sm:space-y-3.5">
  <div class="bg-card rounded-2xl border border-border-line/90 shadow-sm p-4 sm:p-5">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-3 sm:gap-4">
      <div class="space-y-3.5">
        <div>
          <label class="block text-xs font-semibold uppercase tracking-wider text-slate-muted mb-1">Progression Type</label>
          <select id="seq-type" class="w-full px-3.5 py-2.5 bg-page border border-border-line rounded-xl text-ink text-sm focus:outline-none focus:ring-2 focus:ring-brand-accent">
            <option value="ap" selected>Arithmetic Progression (AP)</option>
            <option value="gp">Geometric Progression (GP)</option>
          </select>
        </div>

        <div class="grid grid-cols-3 gap-2">
          <div>
            <label class="block text-xs text-slate-muted mb-1">First Term (a₁)</label>
            <input type="number" id="seq-a" placeholder="e.g. 2" class="w-full px-3 py-2 bg-page border border-border-line rounded-lg text-sm" />
          </div>
          <div>
            <label id="seq-diff-label" class="block text-xs text-slate-muted mb-1">Common Diff (d)</label>
            <input type="number" id="seq-d" placeholder="e.g. 3" class="w-full px-3 py-2 bg-page border border-border-line rounded-lg text-sm" />
          </div>
          <div>
            <label class="block text-xs text-slate-muted mb-1">Number of Terms (n)</label>
            <input type="number" id="seq-n" min="1" max="1000" placeholder="e.g. 10" class="w-full px-3 py-2 bg-page border border-border-line rounded-lg text-sm" />
          </div>
        </div>

        <button id="seq-calc-btn" type="button" class="w-full px-5 sm:px-6 py-3.5 sm:py-4 text-base bg-brand hover:bg-brand-accent text-card font-semibold rounded-xl shadow-sm transition-all">
          Calculate Sequence
        </button>
      </div>

      <div class="flex flex-col justify-between p-4 sm:p-5 bg-page rounded-xl border border-border-line/80">
        <div>
          <div class="text-xs font-semibold text-slate-muted uppercase tracking-wider">n-th Term (aₙ)</div>
          <div id="seq-an" class="text-3xl font-extrabold text-brand mt-1 font-mono break-words">--</div>
          <div id="seq-sum" class="text-xs text-slate-muted font-bold mt-1">Series sum appears here</div>
        </div>

        <div class="pt-3 border-t border-border-line/80 mt-4 text-xs text-slate-muted">
          <span>First 5 Terms: </span>
          <strong id="seq-preview" class="text-ink font-mono break-words">--</strong>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  function calculate() {
    const type = (document.getElementById('seq-type') as HTMLSelectElement)?.value;
    const a = parseFloat((document.getElementById('seq-a') as HTMLInputElement)?.value || '0');
    const d = parseFloat((document.getElementById('seq-d') as HTMLInputElement)?.value || '1');
    const n = parseInt((document.getElementById('seq-n') as HTMLInputElement)?.value || '1', 10);

    const anEl = document.getElementById('seq-an');
    const sumEl = document.getElementById('seq-sum');
    const previewEl = document.getElementById('seq-preview');

    if (type === 'ap') {
      const an = a + (n - 1) * d;
      const sn = (n / 2) * (2 * a + (n - 1) * d);
      const terms: number[] = [];
      for (let i = 0; i < Math.min(5, n); i++) terms.push(a + i * d);

      if (anEl) anEl.textContent = 'a' + n + ' = ' + an;
      if (sumEl) sumEl.textContent = 'Sum of ' + n + ' terms (S' + n + ') = ' + sn;
      if (previewEl) previewEl.textContent = terms.join(', ') + '...';
    } else {
      const r = d; // common ratio
      const an = a * Math.pow(r, n - 1);
      let sn = 0;
      if (r === 1) sn = a * n;
      else sn = a * (1 - Math.pow(r, n)) / (1 - r);

      const terms: number[] = [];
      for (let i = 0; i < Math.min(5, n); i++) terms.push(a * Math.pow(r, i));

      if (anEl) anEl.textContent = 'a' + n + ' = ' + an.toFixed(4);
      if (sumEl) sumEl.textContent = 'Sum of ' + n + ' terms (S' + n + ') = ' + sn.toFixed(4);
      if (previewEl) previewEl.textContent = terms.map(t => t.toFixed(2)).join(', ') + '...';
    }
  }

  document.getElementById('seq-type')?.addEventListener('change', () => {
    const type = (document.getElementById('seq-type') as HTMLSelectElement)?.value;
    const label = document.getElementById('seq-diff-label');
    if (label) label.textContent = type === 'ap' ? 'Common Diff (d)' : 'Common Ratio (r)';

  });


  document.getElementById('seq-calc-btn')?.addEventListener('click', calculate);
</script>