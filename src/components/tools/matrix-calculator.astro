---
export interface Props {
  tool?: any;
}
---

<div class="space-y-3 sm:space-y-3.5">
  <div class="bg-card rounded-2xl border border-border-line/90 shadow-sm p-4 sm:p-5">
    <div class="space-y-3.5">
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <div>
          <label class="block text-xs font-semibold uppercase tracking-wider text-slate-muted mb-1">Matrix A (2×2)</label>
          <div class="grid grid-cols-2 gap-2">
            <input type="number" id="mat-a11" placeholder="e.g. 1" class="px-3 py-2 bg-page border border-border-line rounded-lg text-center font-bold" />
            <input type="number" id="mat-a12" placeholder="e.g. 2" class="px-3 py-2 bg-page border border-border-line rounded-lg text-center font-bold" />
            <input type="number" id="mat-a21" placeholder="e.g. 3" class="px-3 py-2 bg-page border border-border-line rounded-lg text-center font-bold" />
            <input type="number" id="mat-a22" placeholder="e.g. 4" class="px-3 py-2 bg-page border border-border-line rounded-lg text-center font-bold" />
          </div>
        </div>

        <div>
          <label class="block text-xs font-semibold uppercase tracking-wider text-slate-muted mb-1">Matrix B (2×2)</label>
          <div class="grid grid-cols-2 gap-2">
            <input type="number" id="mat-b11" placeholder="e.g. 2" class="px-3 py-2 bg-page border border-border-line rounded-lg text-center font-bold" />
            <input type="number" id="mat-b12" placeholder="e.g. 0" class="px-3 py-2 bg-page border border-border-line rounded-lg text-center font-bold" />
            <input type="number" id="mat-b21" placeholder="e.g. 1" class="px-3 py-2 bg-page border border-border-line rounded-lg text-center font-bold" />
            <input type="number" id="mat-b22" placeholder="e.g. 3" class="px-3 py-2 bg-page border border-border-line rounded-lg text-center font-bold" />
          </div>
        </div>
      </div>

      <div class="flex flex-wrap items-center gap-2 pt-2">
        <button id="mat-calc-btn" type="button" class="w-full px-5 sm:px-6 py-3.5 sm:py-4 text-base bg-brand hover:bg-brand-accent text-card font-semibold rounded-xl shadow-sm transition-all">
          Multiply Matrices (A × B)
        </button>
      </div>

      <div class="p-4 sm:p-5 bg-page rounded-xl border border-border-line/80 flex flex-col sm:flex-row items-center justify-between gap-4">
        <div>
          <div class="text-xs font-semibold text-slate-muted uppercase tracking-wider">Product Matrix (A × B)</div>
          <div id="mat-prod" class="text-2xl font-extrabold text-brand mt-1 font-mono break-words">--</div>
        </div>
        <div class="text-right text-xs text-slate-muted">
          <div>Sum (A + B): <strong id="mat-sum" class="text-ink">[3, 2], [4, 7]</strong></div>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  function calculate() {
    const a11 = parseFloat((document.getElementById('mat-a11') as HTMLInputElement)?.value || '0');
    const a12 = parseFloat((document.getElementById('mat-a12') as HTMLInputElement)?.value || '0');
    const a21 = parseFloat((document.getElementById('mat-a21') as HTMLInputElement)?.value || '0');
    const a22 = parseFloat((document.getElementById('mat-a22') as HTMLInputElement)?.value || '0');

    const b11 = parseFloat((document.getElementById('mat-b11') as HTMLInputElement)?.value || '0');
    const b12 = parseFloat((document.getElementById('mat-b12') as HTMLInputElement)?.value || '0');
    const b21 = parseFloat((document.getElementById('mat-b21') as HTMLInputElement)?.value || '0');
    const b22 = parseFloat((document.getElementById('mat-b22') as HTMLInputElement)?.value || '0');

    const c11 = a11 * b11 + a12 * b21;
    const c12 = a11 * b12 + a12 * b22;
    const c21 = a21 * b11 + a22 * b21;
    const c22 = a21 * b12 + a22 * b22;

    const s11 = a11 + b11;
    const s12 = a12 + b12;
    const s21 = a21 + b21;
    const s22 = a22 + b22;

    const prodEl = document.getElementById('mat-prod');
    const sumEl = document.getElementById('mat-sum');

    if (prodEl) prodEl.textContent = '[' + c11 + ', ' + c12 + ']  |  [' + c21 + ', ' + c22 + ']';
    if (sumEl) sumEl.textContent = '[' + s11 + ', ' + s12 + '], [' + s21 + ', ' + s22 + ']';
  }


  document.getElementById('mat-calc-btn')?.addEventListener('click', calculate);
</script>