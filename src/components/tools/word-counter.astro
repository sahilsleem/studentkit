---
export interface Props {
  tool?: any;
}
---

<div class="space-y-3 sm:space-y-3.5">
  <div class="bg-card rounded-2xl border border-border-line/90 shadow-sm p-4 sm:p-5">
    <div class="space-y-3.5">
      <div class="flex items-center justify-between">
        <label class="block text-xs font-semibold uppercase tracking-wider text-slate-muted">Paste or type your text</label>
        <button id="wc-clear-btn" type="button" class="text-xs text-slate-muted hover:text-slate-muted font-medium">Clear Text</button>
      </div>

      <textarea id="wc-input" rows="8" placeholder="Start typing or paste your essay, assignment, or notes here..." class="w-full px-4 py-3 bg-page border border-border-line rounded-xl text-ink text-sm focus:outline-none focus:ring-2 focus:ring-brand-accent focus:bg-card transition-all"></textarea>
    </div>

    <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-6 gap-3 mt-4">
      <div class="p-4 bg-page rounded-xl border border-border-line/80 text-center">
        <div class="text-xs text-slate-muted font-medium">Words</div>
        <div id="wc-words" class="text-2xl font-bold text-brand font-mono mt-1">--</div>
      </div>
      <div class="p-4 bg-page rounded-xl border border-border-line/80 text-center">
        <div class="text-xs text-slate-muted font-medium">Characters</div>
        <div id="wc-chars" class="text-2xl font-bold text-ink font-mono mt-1">0</div>
      </div>
      <div class="p-4 bg-page rounded-xl border border-border-line/80 text-center">
        <div class="text-xs text-slate-muted font-medium">No Spaces</div>
        <div id="wc-chars-no-space" class="text-2xl font-bold text-ink font-mono mt-1">0</div>
      </div>
      <div class="p-4 bg-page rounded-xl border border-border-line/80 text-center">
        <div class="text-xs text-slate-muted font-medium">Sentences</div>
        <div id="wc-sentences" class="text-2xl font-bold text-ink font-mono mt-1">0</div>
      </div>
      <div class="p-4 bg-page rounded-xl border border-border-line/80 text-center">
        <div class="text-xs text-slate-muted font-medium">Paragraphs</div>
        <div id="wc-paragraphs" class="text-2xl font-bold text-ink font-mono mt-1">--</div>
      </div>
      <div class="p-4 bg-page rounded-xl border border-border-line/80 text-center">
        <div class="text-xs text-slate-muted font-medium">Reading Time</div>
        <div id="wc-read-time" class="text-lg font-bold text-ink font-mono mt-1">0m</div>
      </div>
    </div>
  </div>
</div>

<script>
  const textInput = document.getElementById('wc-input') as HTMLTextAreaElement;
  const wordsEl = document.getElementById('wc-words');
  const charsEl = document.getElementById('wc-chars');
  const charsNoSpaceEl = document.getElementById('wc-chars-no-space');
  const sentencesEl = document.getElementById('wc-sentences');
  const paragraphsEl = document.getElementById('wc-paragraphs');
  const readTimeEl = document.getElementById('wc-read-time');
  const clearBtn = document.getElementById('wc-clear-btn');

  function analyze() {
    const text = textInput?.value || '';
    const words = text.trim() ? text.trim().split(/\s+/).length : 0;
    const chars = text.length;
    const charsNoSpace = text.replace(/\s/g, '').length;
    const sentences = text.trim() ? (text.match(/[^.!?]+[.!?]+/g) || []).length || (text.trim().length > 0 ? 1 : 0) : 0;
    const paragraphs = text.trim() ? text.split(/\n\s*\n/).filter(p => p.trim().length > 0).length || (text.trim().length > 0 ? 1 : 0) : 0;
    const readingTimeMin = Math.ceil(words / 200);

    if (wordsEl) wordsEl.textContent = words.toLocaleString();
    if (charsEl) charsEl.textContent = chars.toLocaleString();
    if (charsNoSpaceEl) charsNoSpaceEl.textContent = charsNoSpace.toLocaleString();
    if (sentencesEl) sentencesEl.textContent = sentences.toLocaleString();
    if (paragraphsEl) paragraphsEl.textContent = paragraphs.toLocaleString();
    if (readTimeEl) readTimeEl.textContent = readingTimeMin + 'm';
  }

  textInput?.addEventListener('input', analyze);
  clearBtn?.addEventListener('click', () => {
    if (textInput) textInput.value = '';
    analyze();
  });
</script>